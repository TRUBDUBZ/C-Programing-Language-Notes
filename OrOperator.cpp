// Or Operator
//
// The `or` logical operator is denoted by `||`
//
// It returns `true` when the condition on the left is `true` or the condition on the `right`. Only one of them needs to be `true`
//
// Note: The keyword or can be used in the place of `||`
//

#include <iostream>

int main() {

  int day = 6;

  if (day == 6 || day == 7) {
    
    std::cout "Weekend!\n";
  

  }

}
