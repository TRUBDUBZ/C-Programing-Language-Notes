#include <iostream>

int main() {

    int grade = 90;

    if(grade != 60) {

      std::cout << "Pass\n";
    }

}

// an if statement is used to test expressions for truth
//  - if the condition evaluates to true, then the code within the block is executed; otherwise it will be skippped


